<Window x:Class="GenericModLauncher.ModLauncher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="Mod Launcher" Height="480" Width="640">
  <Grid Background="Black">
    <Image Source="Assets/test-bg.png"
           Stretch="UniformToFill"
           Panel.ZIndex="0">
      <Image.Effect>
        <BlurEffect Radius="20" />
      </Image.Effect>
    </Image>

    <!-- Other content on top -->
    <Grid Panel.ZIndex="1">
      <!-- Foreground content -->
      <Grid Background="#CC1A1A1E">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--<TextBlock FontFamily="Trebuchet MS" Text="Mod Launcher" FontSize="24" Foreground="White" Margin="10"
                   HorizontalAlignment="Center" />-->
        <Image Source="Assets/test-logo.png" MaxHeight="65" Margin="10,20,10,10" />

        <ListBox Grid.Row="1" Margin="10" Background="#CC2D2D30" Foreground="White">
          <ListBoxItem Content="Mod 1" />
          <ListBoxItem Content="Mod 2" />
          <ListBoxItem Content="Mod 3" />
          <ListBoxItem Content="Mod 4" />
        </ListBox>

        <Button FontFamily="Trebuchet MS" FontStyle="Italic" FontSize="14" Content="Launch Game" Grid.Row="1"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20"
                Padding="10"
                Width="120" />

      </Grid>
    </Grid>
  </Grid>
</Window>
