/**
 * Represents a bounding box in 2D space. The bounding box is its X and Y
 * position, as well as its width and height. The X and Y position represent
 * the top-left corner of the bounding box relative to the origin of the
 * space. The origin is context-specific.
 *
 */
// Auto-generated from C# type BoundingBox
export interface BoundingBox {
    x: number;
    y: number;
    width: number;
    height: number;
    toString(): string;
}
